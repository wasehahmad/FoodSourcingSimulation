public class Layover extends Node{
  
 /**
  * Constructor method for a node which will act as a layover/stop for the truck
  * i.e. a node which is neither a restaurant nor a farm
  */
  public Layover(int name){
    this.nodeName = name;
  }
  
  
}